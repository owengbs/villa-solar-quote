# 角色与任务设定
你是一个资深的前端架构师和全栈开发工程师。请根据我提供的产品需求文档（PRD），为我从零开始构建一款完整的 H5 移动端单页应用（SPA）。
建议使用 React + TypeScript + Tailwind CSS，并结合适当的动画库（如 Framer Motion）来实现。

# 1. 项目概述
- **项目名称**：TCL光伏科技 - 墅野新居案例引导 H5
- **视觉与 UI 定位**：整体视觉方向偏简洁风，配色清新自然（参考现代科技与环保结合的设计，主色调建议为科技蓝或环保绿，搭配大面积留白），需要具有现代化的移动端表单 UI 感（类似向导/Wizard）。
- **核心体验**：支持流畅的页面转场动画，包含状态持久化（筛选条件在页面间同步）。

# 2. 页面与交互需求详细说明

## Page 1: 首页 (Home / Landing)
- **视觉层**：
  - 页面背景需具有视觉冲击力（可使用清新的风景/建筑占位图或纯净的渐变色）。
  - 主标题：“TCL光伏科技”，副标题：“墅野新居”。
- **交互层**：
  - 核心入口是一个“案例指引”按钮。
  - **动画需求**：按钮附近需配有一个“手指点击”的动态提示效果（如上下浮动或呼吸动画），以吸引用户注意力。
  - 点击按钮：使用平滑的路由跳转至 Page 2。

## Page 2: 条件筛选向导页 (Filter Form)
- **顶栏 (Header)**：左侧为返回按钮（点击返回 Page 1），中间标题为“TCL光伏科技”。
- **表单区域 (Form)**：包含三个分类的单选组（Radio Group），UI 需设计为易于手指触控的卡片/标签样式，而非传统的原生单选框。
  1. 房型：独栋、双拼、联排（注：修复了原稿中的重复项）。
  2. 屋面结构：平屋面、斜屋面。
  3. 技术方案：阳光房、贴面安装、轻型支架、阵列式。
- **底部动作条 (Bottom Actions)**：
  - 固定在页面底部，包含一个“查看案例”大按钮。
  - 该按钮同样需要“手指点击”的动态引导效果。
  - 点击后：保存当前选中的筛选状态，并跳转至 Page 3。

## Page 3: 案例结果列表页 (Case List)
- **顶栏与快捷筛选 (Sticky Filter Header)**：
  - 左侧返回按钮（返回 Page 2，且需保留之前的选中状态），中间标题。
  - **紧贴顶栏下方**：实现一个可交互的横向筛选条，展示当前选中的“房型”、“屋面结构”、“技术方案”。用户可以在这里直接点击下拉或使用 Bottom Sheet 再次调整选项，调整后下方列表需实时过滤。
- **案例列表区 (List & Cards)**：
  - 页面主体为垂直滑动的案例列表。
  - **卡片内容**：项目名称（如：杭州XX项目）、详细技术参数（技术方案、光伏容量、储能容量等）。
  - **图片轮播 (Carousel)**：每个案例卡片内可能有多张实景图，需要支持用户在卡片内“右滑查看更多”图片。
  - **全屏大图 (Lightbox)**：单击任何一张图片，需弹出全屏的图片查看器，支持双指缩放和左右滑动切换。

# 3. 工程化与技术要求
1. **状态管理**：请设计一个统一的 Store（可以使用 React Context 或 Zustand）来管理用户的筛选条件，确保 Page 2 和 Page 3 之间的数据联动。
2. **Mock 数据**：请在项目中硬编码提供一个包含至少 4-5 个案例的 Mock JSON 数组。每个案例需包含对应的房型、屋面结构、技术方案字段以及 2-3 张占位图 URL，以便我运行后能直接测试筛选逻辑。
3. **移动端适配**：需完美适配移动端，处理好 iOS 底部安全区（env(safe-area-inset-bottom)）。
4. **代码结构**：请将 UI 拆分为合理的组件（如 `Layout`, `FilterBar`, `CaseCard`, `ImageLightbox` 等），保持代码的高内聚低耦合。

请一步步思考，先搭建基础骨架和路由，然后实现状态管理，最后填充页面细节和动画效果。
